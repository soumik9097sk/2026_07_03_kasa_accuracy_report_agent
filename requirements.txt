# Pinned to the versions currently verified in .venv.
# pandas must stay <3.0 -- see CLAUDE.md ("pandas is pinned below 3.0").
pandas==2.3.3
numpy==2.5.1
openpyxl==3.1.5
google-genai==2.11.0
python-dotenv==1.2.2
matplotlib==3.11.0
python-pptx==1.0.2
